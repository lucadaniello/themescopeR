% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{preprocess_texts}
\alias{preprocess_texts}
\title{Annotate a document collection with udpipe}
\usage{
preprocess_texts(
  collection,
  model,
  text_col = "text",
  doc_id_col = "doc_id",
  batch_size = 500,
  verbose = TRUE
)
}
\arguments{
\item{collection}{Data frame with a text column and a document-id column,
typically the output of \code{\link[=read_collection]{read_collection()}}.}

\item{model}{A \pkg{udpipe} model object, a path to a \code{.udpipe} file, or a
language name (e.g. \code{"english"}) which is downloaded and cached via
\code{\link[=ts_download_model]{ts_download_model()}}.}

\item{text_col}{Name of the text column (default \code{"text"}).}

\item{doc_id_col}{Name of the document-id column (default \code{"doc_id"}).}

\item{batch_size}{Integer. Documents processed per batch (default \code{500}).}

\item{verbose}{Logical. Print progress messages (default \code{TRUE}).}
}
\value{
A data frame with columns \code{doc_id}, \code{sentence_id} (globally unique),
\code{token}, \code{lemma} (lower-cased) and \code{upos}.
}
\description{
Tokenises, lemmatises and POS-tags a collection of raw documents using a
\pkg{udpipe} language model, returning a tokens data frame ready for
\code{\link[=themescope]{themescope()}} and the rest of the backend.
}
\examples{
\dontrun{
coll   <- read_collection("sample_collection.csv")
tokens <- preprocess_texts(coll, model = "english")
result <- themescope(tokens)
}

}
