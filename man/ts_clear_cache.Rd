% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{ts_clear_cache}
\alias{ts_clear_cache}
\title{Delete cached language models}
\usage{
ts_clear_cache(model_dir = NULL, ask = interactive())
}
\arguments{
\item{model_dir}{Directory to clear. Defaults to \code{\link[=themescope_cache_dir]{themescope_cache_dir()}}.}

\item{ask}{Logical. Ask for confirmation before deleting. Defaults to
\code{\link[=interactive]{interactive()}}.}
}
\value{
The paths that were removed, invisibly (\code{character(0)} if the cache
was empty or the user declined).
}
\description{
Removes the models and the registry downloaded by \code{\link[=ts_download_model]{ts_download_model()}} and
\code{\link[=ts_list_models]{ts_list_models()}}, and the cache directory itself. Nothing else in the
package writes persistently, so this clears everything themescopeR has ever
stored on disk.
}
\examples{
# Point the cache at a throwaway directory and clear it again
d <- file.path(tempdir(), "themescope-cache-example")
dir.create(d, showWarnings = FALSE)
file.create(file.path(d, "example-ud-2.15.udpipe"))
ts_clear_cache(model_dir = d, ask = FALSE)
dir.exists(d)

}
\seealso{
\code{\link[=themescope_cache_dir]{themescope_cache_dir()}}, \code{\link[=ts_download_model]{ts_download_model()}}.
}
