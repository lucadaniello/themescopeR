% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archive.R
\name{save_themescope}
\alias{save_themescope}
\title{Save an analysis as a \code{.themescope} archive}
\usage{
save_themescope(
  x,
  file,
  words = NULL,
  collection = NULL,
  meta = list(),
  compress = "xz"
)
}
\arguments{
\item{x}{A \code{themescope} object, as returned by \code{\link[=themescope]{themescope()}}.}

\item{file}{Destination path. The extension \code{.themescope} is added when
\code{file} has none.}

\item{words}{Optional annotated words data frame (the \code{\link[=preprocess_texts]{preprocess_texts()}}
output the analysis was run on). Storing it lets the archive be re-analysed
with different parameters.}

\item{collection}{Optional raw document collection (the \code{\link[=read_collection]{read_collection()}}
output), stored so the original texts travel with the results.}

\item{meta}{Optional named list of extra information to record, for example
\code{list(language = "english", treebank = "GUM", lexicon = "Brysbaert")}.
Whatever is supplied is shown by \code{\link[=summary]{summary()}} and by the import window of
the Shiny GUI.}

\item{compress}{Compression passed to \code{\link[=saveRDS]{saveRDS()}} (default \code{"xz"}).}
}
\value{
The path of the written file, invisibly.
}
\description{
Writes a finished analysis, together with the data it was computed from, to a
single \code{.themescope} file. The archive is a compressed RDS holding the
\code{themescope} object, the annotated words it was built on, the raw collection
(optional), the term-level table with the scores used to draw the maps, the
community-level table, and the parameters of the run.

Reopening the archive with \code{\link[=read_themescope]{read_themescope()}} restores maps, communities and
top terms without re-annotating the corpus or re-running the pipeline.
}
\examples{
\dontrun{
result <- themescope(tokens, seed = 1)
save_themescope(result, "climate_study.themescope",
                words = tokens, collection = coll,
                meta = list(language = "english", lexicon = "Brysbaert"))
}

}
\seealso{
\code{\link[=read_themescope]{read_themescope()}} to load an archive back.
}
