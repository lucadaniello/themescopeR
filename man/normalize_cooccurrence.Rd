% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_utils.R
\name{normalize_cooccurrence}
\alias{normalize_cooccurrence}
\title{Normalise a co-occurrence matrix into a similarity matrix}
\usage{
normalize_cooccurrence(
  cooc_matrix,
  presence,
  method = c("association", "equivalence", "jaccard", "salton", "inclusion", "frequency")
)
}
\arguments{
\item{cooc_matrix}{Symmetric sparse \code{Matrix} of co-occurrence counts.}

\item{presence}{Named numeric vector of term presences \eqn{a_t}; names must
match the rows/columns of \code{cooc_matrix}.}

\item{method}{One of \code{"association"} (default), \code{"equivalence"}, \code{"jaccard"},
\code{"salton"}, \code{"inclusion"}, \code{"frequency"}.}
}
\value{
A sparse \code{dgCMatrix} of the same dimensions as \code{cooc_matrix}.
}
\description{
Re-weights raw co-occurrence counts with one of several similarity measures
commonly used in co-word analysis. Given the co-occurrence count
\eqn{c_{ij}} and the term presences \eqn{a_i, a_j}:
\describe{
\item{\code{"association"}}{\eqn{c_{ij} / (a_i a_j)}, Association Strength
(ThemeScope default; van Eck & Waltman).}
\item{\code{"equivalence"}}{\eqn{c_{ij}^2 / (a_i a_j)}, equivalence index.}
\item{\code{"jaccard"}}{\eqn{c_{ij} / (a_i + a_j - c_{ij})}.}
\item{\code{"salton"}}{\eqn{c_{ij} / \sqrt{a_i a_j}}, cosine or Salton's measure.}
\item{\code{"inclusion"}}{\eqn{c_{ij} / \min(a_i, a_j)}.}
\item{\code{"frequency"}}{the raw counts, unchanged.}
}
}
\examples{
m <- Matrix::sparseMatrix(
  i = c(1, 2), j = c(2, 1), x = c(3, 3), dims = c(3, 3),
  dimnames = list(c("a", "b", "c"), c("a", "b", "c"))
)
normalize_cooccurrence(m, c(a = 5, b = 4, c = 2), method = "jaccard")

}
