% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/community.R
\name{detect_communities}
\alias{detect_communities}
\title{Detect communities in a semantic network}
\usage{
detect_communities(
  graph,
  algorithm = "walktrap",
  steps = 4,
  resolution = 1,
  min_size = 10,
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{graph}{An undirected weighted \code{igraph} object, e.g. from
\code{\link[=build_cooccurrence_network]{build_cooccurrence_network()}}.}

\item{algorithm}{Character. One of \code{"walktrap"} (default), \code{"louvain"} or
\code{"leiden"}.}

\item{steps}{Integer. Number of steps for the walktrap random walk (default
\code{4}). Used only when \code{algorithm = "walktrap"}.}

\item{resolution}{Numeric resolution parameter for \code{"louvain"} and \code{"leiden"}
(default \code{1}); higher values yield more, smaller communities. Ignored by
walktrap.}

\item{min_size}{Integer. Minimum number of members for a community to be
retained; smaller communities are assigned \code{NA} membership (default \code{10}).}

\item{seed}{Optional integer. If supplied, sets the random seed before
community detection for reproducibility (Louvain and Leiden are stochastic).}

\item{verbose}{Logical. Print a progress message (default \code{TRUE}).}
}
\value{
A named list with:
\describe{
\item{\code{membership}}{Named integer vector mapping each vertex to its
community ID (\code{NA} for vertices in removed small communities).}
\item{\code{communities}}{Named list of character vectors of member names, one
per retained community (\code{"C1"}, \code{"C2"}, ...).}
\item{\code{algorithm_result}}{The raw \code{communities} object from igraph.}
}
}
\description{
Runs walktrap or Louvain community detection on a weighted undirected graph
and filters out small communities.
}
\examples{
words <- readRDS(system.file("extdata", "demo_annotated.rds",
                             package = "themescopeR"))
vocab <- build_vocab(words, vocab_size = 300)
cooc  <- build_cooccurrence_matrix(words, vocab = vocab)
net   <- build_cooccurrence_network(cooc$cooc_matrix, verbose = FALSE)
comm <- detect_communities(net, algorithm = "walktrap", min_size = 5, seed = 1)
lengths(comm$communities)

comm <- detect_communities(net, algorithm = "louvain", resolution = 1,
                           min_size = 5, seed = 1)
lengths(comm$communities)

}
