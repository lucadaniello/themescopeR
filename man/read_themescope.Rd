% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archive.R
\name{read_themescope}
\alias{read_themescope}
\alias{print.themescope_archive}
\alias{summary.themescope_archive}
\title{Read a \code{.themescope} archive}
\usage{
read_themescope(file)

\method{print}{themescope_archive}(x, ...)

\method{summary}{themescope_archive}(object, ...)
}
\arguments{
\item{file}{Path to a \code{.themescope} file.}

\item{x}{A \code{themescope_archive} object.}

\item{...}{Ignored.}

\item{object}{A \code{themescope_archive} object.}
}
\value{
An object of class \code{themescope_archive}: a list with \code{result} (the
\code{themescope} object), \code{words}, \code{collection}, \code{terms}, \code{communities},
\code{meta}, \code{created}, \code{package_version} and \code{version}.
}
\description{
Loads an analysis written by \code{\link[=save_themescope]{save_themescope()}}. Use \code{\link[=summary]{summary()}} on the
result for a table describing what the file contains, and \code{archive$result}
for the \code{themescope} object itself (which plots, prints and coerces to a data
frame as usual).
}
\section{Functions}{
\itemize{
\item \code{print(themescope_archive)}: Print a one-screen overview of the archive.

\item \code{summary(themescope_archive)}: Describe the archive as a two-column data frame
(\code{field}, \code{value}), covering the corpus, the parameters of the run, the
vocabulary, the network and the lexicon. This is what the Shiny import
window displays.

}}
\examples{
\dontrun{
archive <- read_themescope("climate_study.themescope")
summary(archive)
plot(archive$result, type = "map", label = "terms")
}

}
\seealso{
\code{\link[=save_themescope]{save_themescope()}}.
}
