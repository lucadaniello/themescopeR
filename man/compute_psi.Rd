% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{compute_psi}
\alias{compute_psi}
\title{Compute the Prototypical Salience Index (PSI) per community}
\usage{
compute_psi(graph, communities, presence)
}
\arguments{
\item{graph}{An undirected weighted \code{igraph} object (the full network).
Edge weights are the Association Strength values.}

\item{communities}{Named list of character vectors of community members, as
returned by \code{\link[=detect_communities]{detect_communities()}}.}

\item{presence}{Named numeric vector of term presence \eqn{a_t} (sentence
counts), as returned in the \code{presence} element of
\code{\link[=build_cooccurrence_matrix]{build_cooccurrence_matrix()}}. Names must match vertex names in \code{graph}.}
}
\value{
A named numeric vector of PSI values, one per community.
}
\description{
The PSI operationalises \emph{anchoring} in Social Representation Theory: the
degree to which a thematic community is structurally embedded in the wider
discourse. Following the ThemeScope papers it is defined as
\deqn{\Psi(g_i) = \frac{\sum_{t \in g_i} a_t \cdot s_t}{\max_j \delta(g_j)},}
where \eqn{a_t} is the \emph{presence} of term \eqn{t} (the number of sentences
containing it), \eqn{s_t = \frac{\sum_{t' \in N_i(t)} AS_{t,t'}}{|N_i(t)|}} is
the \emph{mean association strength} of \eqn{t}'s neighbours within the community
subgraph, and \eqn{\delta(g_i) = \frac{2 W_i}{|V_i|(|V_i|-1)}} is the weighted
internal density of community \eqn{i} (\eqn{W_i} = sum of internal edge
weights).
}
\details{
This implementation follows the equations in the ThemeScope manuscript
(Eqs. 2--4) and case study (Eq. 2): the per-term contribution uses the
\strong{mean neighbour association strength} \eqn{s_t} (not the raw degree) and the
normalisation uses the \strong{maximum weighted community density} (not the maximum
numerator). Because the denominator is a single global constant, the
subsequent z-scoring used for the ThemeScope map is unaffected by it.
}
\examples{
\dontrun{
psi <- compute_psi(net, comm$communities, cooc$presence)
}

}
