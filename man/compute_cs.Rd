% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{compute_cs}
\alias{compute_cs}
\title{Compute the Concreteness Score (CS) per community}
\usage{
compute_cs(graph, communities, concreteness_lexicon = brysbaert)
}
\arguments{
\item{graph}{An undirected weighted \code{igraph} object with a \code{weight} edge
attribute.}

\item{communities}{Named list of character vectors of community members.}

\item{concreteness_lexicon}{Data frame with columns \code{"word"} and \code{"conc.m"}
(1--5 scale). Defaults to the bundled \link{brysbaert} norms.}
}
\value{
A named numeric vector of CS values. Communities where no edge has
both endpoints in the lexicon return \code{NA}.
}
\description{
The CS operationalises \emph{objectification}: the degree to which a community is
grounded in concrete, perceptually accessible content. It is the
association-strength-weighted mean concreteness of edge endpoints
\deqn{C_w(g_i) = \frac{\sum_{(t,t') \in E_i} w_{t,t'} \cdot \frac{c(t)+c(t')}{2}}{\sum_{(t,t') \in E_i} w_{t,t'}},}
where \eqn{w_{t,t'}} is the AS edge weight and \eqn{c(t)} the concreteness
rating. Only edges where \strong{both} endpoints have a rating contribute.
}
\examples{
\dontrun{
cs <- compute_cs(net, comm$communities)
}

}
