% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{validate_words_df}
\alias{validate_words_df}
\title{Validate an annotated words data frame}
\usage{
validate_words_df(words_df)
}
\arguments{
\item{words_df}{Data frame to validate. Must contain \code{doc_id},
\code{sentence_id}, \code{upos}, and at least one of \code{token} or \code{lemma}.}
}
\value{
Invisibly returns \code{TRUE} if validation passes.
}
\description{
Checks that an annotated data frame has all columns required by the
ThemeScope backend. The "word" used downstream is either the \code{token} or the
\code{lemma} column (chosen via the \code{unit} argument of \code{\link[=build_vocab]{build_vocab()}} /
\code{\link[=build_cooccurrence_matrix]{build_cooccurrence_matrix()}}), so at least one of them must be present.
Emits informative errors via \pkg{cli} when columns are missing.
}
\examples{
df <- data.frame(doc_id = 1, sentence_id = 1, token = "dog", upos = "NOUN")
validate_words_df(df)

}
