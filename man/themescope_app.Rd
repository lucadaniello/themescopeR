% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/themescope_app.R
\name{themescope_app}
\alias{themescope_app}
\title{Launch the themescopeR Shiny application}
\usage{
themescope_app(display_mode = "normal", max_upload_size_mb = NULL, ...)
}
\arguments{
\item{display_mode}{Passed to \code{\link[shiny:runApp]{shiny::runApp()}} (\code{"normal"} by default;
\code{"showcase"} displays the app code alongside it).}

\item{max_upload_size_mb}{Optional numeric. Baseline maximum file-upload size
(in megabytes) for the GUI. Shiny's own default is only 5 MB, far too small
for typical ThemeScope collections, so the app already raises this to 50 MB.
Set a larger value here to lift the baseline further from R (e.g. \code{2048} for
2 GB). The app also raises it interactively: picking a file larger than the
current limit opens a window that suggests a new limit, warns about the
memory implications, and imports the file once confirmed.}

\item{...}{Further arguments passed to \code{\link[shiny:runApp]{shiny::runApp()}} (e.g. \code{port},
\code{launch.browser}).}
}
\value{
Called for its side effect of launching the app; does not return a
useful value.
}
\description{
Opens the optional graphical interface to the ThemeScope pipeline. The app is
a thin front-end: it delegates all computation to the exported package
functions (\code{\link[=read_collection]{read_collection()}}, \code{\link[=preprocess_texts]{preprocess_texts()}}, \code{\link[=themescope]{themescope()}},
\code{\link[=top_terms]{top_terms()}}, \code{\link[=save_themescope]{save_themescope()}}, \code{\link[=read_themescope]{read_themescope()}}), so the GUI
reproduces the console results exactly.
}
\details{
The interface follows the pipeline step by step: import (raw text, a saved
\code{.themescope} archive, or the bundled demo corpus, which ships already
annotated), preprocess, run, and refine the look of the network without
recomputing anything. Results are shown as an interactive map and network, a
filterable community table and per-cluster term lists, and the whole study can
be exported as a \code{.themescope} archive.

The GUI requires a few additional packages declared in \code{Suggests}:
\pkg{shiny}, \pkg{bslib}, \pkg{DT}, \pkg{plotly}, \pkg{visNetwork} and
\pkg{shinycssloaders}. Install any that are missing with
\code{install.packages()}.
}
\examples{
\dontrun{
themescope_app()
}

}
