% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{read_collection}
\alias{read_collection}
\title{Read a document collection into a tidy data frame}
\usage{
read_collection(path, text_col = NULL, id_col = NULL, sequential_ids = FALSE)
}
\arguments{
\item{path}{Path to a single file or a \code{.zip} archive.}

\item{text_col}{Optional name of the text column. If \code{NULL}, detected from
common names (\code{text}, \code{body}, \code{content}, \code{comment}, \code{message}) or, failing
that, the sole character column.}

\item{id_col}{Optional name of the document-id column. If \code{NULL}, detected
from common names (\code{doc_id}, \code{id}, \code{document}, ...); if none is found, a
sequential \code{doc_id} is generated (\code{doc_1}, \code{doc_2}, ...).}

\item{sequential_ids}{Logical (default \code{FALSE}). If \code{TRUE}, documents are
always numbered \code{doc_1}, \code{doc_2}, ... The identifier that came with the file
is not discarded: it is kept as a \code{source_id} column. Useful when the source
ids are long hashes that clutter the tables (this is what the Shiny GUI
uses).}
}
\value{
A data frame with \code{doc_id} (character) and \code{text} (character) as the
first two columns, followed by any remaining source columns. Duplicated ids
are made unique with a warning.
}
\description{
Imports a corpus of raw documents from a single file or a zip archive of many
files, returning a tidy data frame with a \code{doc_id} and a \code{text} column (plus
any metadata columns present in the source).
}
\details{
Supported single-file formats: \code{.csv}, \code{.tsv}, \code{.txt}, \code{.xlsx}/\code{.xls}, and
\code{.RData}/\code{.rda}. A \code{.zip} may contain any number of these (e.g. many \code{.csv}
or many \code{.txt}); all are read and row-bound. For \code{.txt} files, \strong{each file is
treated as one document} (its \code{doc_id} is the file name); this makes a zip of
\code{.txt} files a natural multi-document collection.
}
\examples{
# The bundled 1000-document sample
path <- system.file("extdata", "sample_collection.csv", package = "themescopeR")
coll <- read_collection(path)
head(coll$doc_id, 3)

# Readable ids; the identifier from the file survives as `source_id`
coll <- read_collection(path, sequential_ids = TRUE)
head(coll[, c("doc_id", "source_id")], 3)

\dontrun{
# Any single file, or a zip holding many (one document per .txt)
coll <- read_collection("sample_collection.xlsx")
coll <- read_collection("texts.zip")
}

}
