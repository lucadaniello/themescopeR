% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{lexicon_coverage}
\alias{lexicon_coverage}
\title{Concreteness lexicon coverage diagnostics}
\usage{
lexicon_coverage(x, lexicon = brysbaert)
}
\arguments{
\item{x}{Either a \code{themescope} object (coverage is reported per community and
for the whole network) or a character vector of terms.}

\item{lexicon}{Data frame with columns \code{"word"} and \code{"conc.m"}. Defaults to
the bundled \link{brysbaert} norms.}
}
\value{
A data frame with columns \code{community} (community id, or \code{"(all)"} for
the overall row), \code{n_terms}, \code{n_matched} and \code{coverage} (proportion in
\eqn{[0, 1]}).
}
\description{
Reports how many terms have a rating in a concreteness lexicon — overall and,
for a \code{\link[=themescope]{themescope()}} result, per community. A low coverage means the
Concreteness Score (CS) is computed on few edges and may be unreliable; this
typically happens when the corpus language does not match the lexicon (the
bundled \link{brysbaert} norms cover \strong{English} words only).
}
\examples{
lex <- data.frame(word = c("dog", "cat"), conc.m = c(4.8, 4.7))
lexicon_coverage(c("dog", "cat", "freedom"), lex)

}
\seealso{
\code{\link[=compute_cs]{compute_cs()}}, \code{\link[=match_concreteness]{match_concreteness()}}.
}
